# Greeter

Greeter is a simple template for a Java/Maven project with a CLI and GUI
application using a common core.

This template exists as an example of using [Metalpet](../../README.md) and is
not intended to serve as a real template.
